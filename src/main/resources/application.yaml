spring:
  application:
    name: itau
  datasource:
    url: jdbc:postgresql://localhost:5432/api-key
    username: x
    password: x
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org.springframework.security: TRACE
